# pyproject.toml

[project]
name = "agente-medico"
version = "0.1.0"
dependencies = [
    "langchain",
    "langgraph",
    "langchain-google-genai",
    "python-dotenv"
]

[tool.langserve]
packages = [
    { name = "agent", location = "packages/agent" },
]